# 작업: 룰원칙해설.md 전수 감사 — 옛정보·누락 룰 설명 갱신 (사용자 문서)

> 한국어로 응답·보고할 것. 저장소: `C:\Users\ghdtj\workspace\portfolio\local-ai-assist`, 브랜치 `develop`.
> ⚠️ **선행 조건: R-DOC-A(DETECTION_RULES.md 감사)가 완료된 뒤에 시작할 것** — 이 문서는
> DETECTION_RULES.md에서 파생되므로 원본이 정리된 후에 맞춰야 한다.
> ⚠️ **코드·설정·테스트 수정 절대 금지** (`src/`, `config/`, `tests/` 읽기 전용).

## 1. 목표

- `docs/guide/룰원칙해설.md`(460줄, 비회계 사용자용 "왜 신호인가" 해설)를 현행
  DETECTION_RULES.md와 전수 대조해: ① 옛정보 정정 ② 누락된 최신 변경의 사용자용 설명 추가
  ③ DETECTION_RULES와의 모순 0을 만든다.
- 성공 기준: §6 검증 통과 + 41룰 커버리지 표 + 신규 반영 3건(아래 §3-2)이 사용자 언어로 추가됨.

## 2. 컨텍스트

- 감사 대상: `docs/guide/룰원칙해설.md` — 구조: "룰 전체 지도"(레이어 표) + 룰별 해설 카드.
- 대조 기준(읽기 전용): `docs/spec/DETECTION_RULES.md` (권위 SoT — R-DOC-A 감사 완료본),
  `docs/spec/RULE_DETAIL_METADATA_V1_LOCK.md` (canonical 32 정의).
- 이 문서의 성격 (위반하면 실패):
  - 독자는 **비회계 사용자**다. 점수 산식·파라미터·구현 경로를 넣지 않는다 (그건
    DETECTION_RULES 몫 — 이 문서는 "왜"와 "숨은 도메인 가정"만).
  - 톤: 기술문서 평서체. 구어체·감정적·의인화·과장 표현 금지.
  - "잡는다/신호다" = "검토 후보로 올린다"는 PHASE1 역할 원칙 서술(문서 상단)은 불변 원칙.
- 배경 — 최근 변경 중 이 문서에 반영돼야 할 후보 (사용자 관점에서 의미 있는 것만):
  1. **L4-06에 4번째 신호 추가**: "자동 전표라고 적혀 있는데 배치 흔적(batch_id/job_id)이 없고
     같은 날 동류도 거의 없는 단독 전표" = 자동인 척 위장 의심. 왜 신호인가: 정상 자동 전표는
     무리지어 다닌다.
  2. **위장 게이트 (L3-06·L1-05·L1-04·L4-05 공통)**: "자동 전표는 정상"이라는 감면/예외를
     받으려면 진짜 자동이어야 한다 — 위장 의심 전표는 감면을 받지 못한다. 왜: source 칸은
     사람이 적을 수 있는 값이라, 한 칸 위조로 감시를 빠져나가는 비대칭을 막는다.
  3. **자동 결산 배치의 콤보 면제**: 자동 전표의 "승인란 공백+결산기+고액"은 정상 운영 모습이라
     위험 조합 승격에서 제외된다 (정상 medium 노이즈 감소). 단 위장 의심은 면제를 못 받는다.
- 반영하지 말 것: 진행 중 미완 변경 (L2-02 fallback 구현, unknown_approver — 코드 합류 후
  별도 갱신 예정. 지금 쓰면 문서가 코드를 앞서간다).

## 3. 설계 (이대로 수행 — 임의 변경 금지)

1. **전수 대조**: 룰별 해설 카드 41개(canonical 32 + 보조 IC/GR/D, EV 언급부)에 대해:
   ① 룰명/한 줄 설명이 DETECTION_RULES와 일치 ② 해설 내용이 현행 동작과 모순 없음
   ③ 누락 룰 없음 (지도 표의 개수와 카드 수 일치)
2. **신규 반영 3건**(§2 배경 1~3)을 해당 룰 카드에 사용자 언어로 1~3문장씩 추가.
   점수·함수명·설정 키 금지. "왜 신호인가" 화법 유지.
3. 발견한 모순 중 어느 쪽이 맞는지 불명한 것은 수정하지 말고 보고서에 질문으로 기재.
- 설계가 현장과 안 맞으면(예: 문서 구조가 룰 카드 형식이 아님): STATUS: NEEDS_CONTEXT로 멈출 것.

## 4. 단계 체크리스트 (순서 고정 — 건너뛰기·합치기 금지)

- [ ] Step 1: 커버리지 확인 — 지도 표 vs 실제 카드 수 vs DETECTION_RULES 룰 목록 3자 대조
      증거: `rg -c "^#### |^### " docs/guide/룰원칙해설.md` + 누락/잉여 룰 목록 (없으면 "0")
- [ ] Step 2: 41룰 전수 대조 (③축: 명칭 일치 / 내용 모순 / 누락) → 41행 검증표
      증거: 검증표 전체 + "모순" 판정마다 양 문서 인용 1쌍
- [ ] Step 3: 옛정보 정정 + 신규 3건 반영 — **부분 수정만, 전체 재작성·재포맷 금지**
      증거: 항목별 "변경 전 → 변경 후" 목록
- [ ] Step 4(항상 마지막): 전체 검증(§6) 실행 후 출력 원문 확보
※ 각 단계의 증거는 완료 보고에 원문 그대로 포함한다. 증거가 없는 단계는 미수행으로 간주한다.

## 5. 금지 사항 (1건이라도 위반 시 작업 전체 실패)

- 코드·설정·테스트·다른 문서 수정 금지: 수정 가능 파일 = `docs/guide/룰원칙해설.md` **하나뿐**.
- **한글 인코딩 가드**: 파일 전체 재작성, 셸 리다이렉션 일괄 치환, 포매터 금지. 부분 edit만.
- 점수 산식·임계값·함수명·config 키를 이 문서에 넣지 말 것 (독자·역할 위반).
- 미완 변경(L2-02 fallback·unknown_approver) 선반영 금지.
- 문서 상단 PHASE1 역할 원칙 서술·문서 역할 정의 수정 금지.
- 체크리스트 항목 생략·순서 변경 금지. 실패·미완을 완료로 보고 금지.

## 6. 최종 검증 (완료 선언 전 필수 실행)

- mojibake 스캔: `uv run python -c "s=open('docs/guide/룰원칙해설.md',encoding='utf-8').read(); bad=[t for t in (chr(0xFFFD),'?ㅽ','?λ') if t in s]; print('mojibake:', bad or 'NONE')"`
  → 기대: `mojibake: NONE`
- 커버리지: 검증표 41행 전부 판정 기록 (빈 셀 0)
- `git diff --stat docs/guide/룰원칙해설.md` → 기대: 이 파일만 변경
- 상대링크 실재 확인: 문서 내 링크 전수 (파일 작아서 전수 가능)
※ 하나라도 기대와 다르면 DONE 금지. 원인 미상이면 BLOCKED로 보고.

## 7. 완료 보고 양식 (이 양식 그대로, 항목 생략 금지)

STATUS: DONE | DONE_WITH_CONCERNS | NEEDS_CONTEXT | BLOCKED
체크리스트: 항목별 [x]/[ ] + 각 항목의 증거(명령 + 출력 원문 붙여넣기)
41룰 검증표: <전 행 판정 — 생략 금지>
변경 목록: <항목별 변경 전→후>
모순/불명 질문 목록: <수정 안 한 것 — 없으면 "없음">
변경 파일: <docs/guide/룰원칙해설.md 하나여야 함>
최종 검증 결과: §6 명령별 출력 원문
미완·우회·우려 사항: <정직하게 전부. 없으면 "없음">

신뢰 규칙: 부분 실패의 정직한 보고(DONE_WITH_CONCERNS/BLOCKED)는 정상 경로이며 다음 지시로
이어진다. 거짓 DONE은 재검증에서 반드시 드러나고 작업 전체를 처음부터 재수행하게 된다.
